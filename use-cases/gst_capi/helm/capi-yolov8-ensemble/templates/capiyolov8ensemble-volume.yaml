apiVersion: v1
kind: PersistentVolume
metadata:
  name: capiyolov8ensemble-volume
  labels:
    type: local
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/results"
